// finish this file to make it only with button

import React from 'react';
import { View, Button } from 'react-native';

const AdminLogsScreen = () => {
    return (
        <View>
            <Button title="Export Dane to tutaj" />
        </View>
    );
};

export default AdminLogsScreen;
